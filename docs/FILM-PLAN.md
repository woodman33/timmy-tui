# Film plan · Study 03 / spatial-t5k1 phase 2

This addendum feeds the existing DEMO PORTFOLIO `FILM-PLAN-v2.md`. It does not
replace that plan's unresolved TUI captures, founder footage or license holds.
Part C is retained as [communication reference](reference/study03/COMMUNICATION.md).

Opening line: **Build with dimensions. Explain every change.**

Supporting lines, used with their stated scope:

- **See what changed. Know what was checked.** Pair the 72 native bore checks
  with the separate analytical wall-gap result and two unmeasured questions.
- **Turn relationships into editable parts.** Use only with the admitted tray
  recipe's actual native rebuild; the envelope inspector performs no CAD build.
- **One part. Several views. The same meaning.** Show the part/world frame chip
  and mm/m conversion against the same retained source revision.

Proposed sequence: named Bore A / tool.A → coordinate chip → `timmy inspect
clearance` at radius 4.5 mm (2.5 mm gap) → retained negative-control report at
radius 5.5 mm (1.5 mm gap, FAIL against the required **2 mm**) → coverage →
three arrows with distinct meanings. Materialization stays proposed; dependency
and hypothesis arrows do not execute anything.

The frozen study's original 1 mm example remains historical reference. Do not
use that number as the phase-2 acceptance threshold. Show “analytical horizontal
wall gap”; never imply full swept-path clearance or physical validation.

Receipt/shot bindings: `docs/orders/spatial-t5k1/phase2/evidence.json` names the
native prediction/result and each clearance prediction/result. Exact new TUI
capture frames remain pending; the JSON receipts are not screenshots. Select
one negative-control frame from the 1.5 mm failed report when capturing.

Part C's original public copy described an unintegrated research study. That is
still the scope of the frozen artifact. The phase-2 CLI integration is a new,
bounded increment and does not establish integrations with Hana, Spline, Viser,
Rerun, or other proposed surfaces.

Retained CLI text captures now exist in `docs/orders/spatial-t5k1/phase2/`:
`negative-capture.json` shows the 1.5 mm failure and `frame-capture.json` shows
the World/metres/90° frame chip. These are exact CLI outputs, not raster frames
or complete five-moment TUI footage. Their hashes are bound by the phase-2 seal.
