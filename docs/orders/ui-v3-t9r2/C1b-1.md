# ORDER ui-v3-t9r2 · C1b-1 — re-point meaning · HOLD

C1b was split at C1a. C1b-1 re-points every paint site whose *meaning* the law changes; C1b-2 puts the five evidence states on every element that carries evidence; C1c restores the ten label/row pairs the single grey flattened.

## The rule applied

The law admits four accents with fixed meaning and nothing else. Two things the old palette coloured have no hue under it: **interaction** (focus, selection, the active tab — white since C1a) and **attention** (running, next, needs you, blocked, a warning). C1b-1 makes attention white too, with bold or a glyph carrying the emphasis, and sends the **not-yet class** (queued, paused, not installed, idle) to grey-3. SEAL green now paints only what the chain confirms; REFUSE red only refusals and gate fails; PREDICT amber is reserved for forecasts and is painted nowhere yet; GENERATED violet nowhere yet.

## What changed (≈60 sites)

- **`src/tui/theme.ts`:** `warn` is bound to white. Every site that read "amber = pending" now reads white; the dim-class sites below were re-pointed explicitly.
- **Not-yet → grey-3:** StatusGlyph `queued`; Pill kind `warn` (PLANNED, NEEDS KEY, NOT RUNNING, n/3 SURFACES, +offset pills; `accent` pills are bold now so LIVE/ARMED/READY stay distinct); ClipPanel "agent layer missing"; DispatchRail `armed no`; FilesPanel note; BrowsePanel "carbonyl not found"; LogsPanel paused tail; EmptyState extra line.
- **Decorative SEAL → white:** the active tab (bold), the QR text, the SEAL-confirm title, the `==` overlay headings (bold), the flash line, `● fleet connected` / `● bus live` (white ● vs grey ○), lane available, the spend line, node reachable, HANDS `HOLD`, the key sheet's key names (bold white), ChatSurface's PROOF label, StatusGlyph `completed` (✓ white — done is not sealed; `sealed` ● keeps the seal). The header's chain segment is seal **only when the chain verifies**, grey otherwise (it was always green).
- **Attention stays white with a second channel:** the journey `▶` next step is bold; NEEDS YOU / running / blocked / `[WARN]` lines / `⊘` lines are white as before but no longer amber.
- **Mode badge:** NORMAL is white on the line grey; INSERT and CHAT are black on white (inverse, bold) — a human-present mode is the thing you are in.
- **Outside src/tui:** `cli.tsx` and `headless.ts` paint through the theme's `colors` (tool call and result white, error refuse, prompt white; the greeting's cyan is plain bold); `src/tui-opentui/spike.ts` and `scripts/timmy-ui-smoke.tsx` use theme tokens; `src/utils/markdown.ts` (code, bullets, links) and `src/utils/logger.ts` (levels) use `chalk.hex(theme.*)` or a modifier; `src/utils/logserver.ts` paints a denied receipt REFUSE red (it was amber); `src/utils/chatpage.html`'s seven Clearinghouse hexes are the law's values.
- **The gate follows:** `tests/design-contract.test.ts` now walks `src/tui-opentui`, `src/forge`, `cli.tsx`, `headless.ts`, `scripts/timmy-ui-smoke.tsx` and five `src/utils` files with the same checker, and requires chatpage.html's hex set to be a subset of the law. **Declared debt, still outside the gate:** `src/utils/slash-commands.ts` (20 raw SGR escapes in a help renderer) and `src/utils/test-sandboxes.ts` (18 chalk colour members in a dev tool) — counted, not hidden.

## Review

Two adversarial lenses (law compliance; visible regressions by ANSI capture — workflow wf_954d6bd7-ec1). Both confirmed the paint is law-clean: across every captured frame the only hues emitted are SEAL green on chain-confirmed elements, REFUSE red on refusals, and white with the derived greys; no PREDICT amber, no GENERATED violet, no stray hue. The six requested distinctions hold in the escape sequences (active tab, chain segment conditional on chain.ok, both mode badges, the journey ▶ row, the ● / ○ status dots, the dimmed CHAT underlay, the key sheet).

Must-fix items found and applied before sealing: the SetupPanel NEEDS KEY pill and the LanesPanel "waiting on you" pill had been sent to grey-3 although they ask the operator to act (now white, bold); the HANDS needs-approval cell had become byte-identical to HOLD and running (bold now); the ChatSurface model badge had lost its only local-versus-remote signal (a ⌂ prefix now marks a local model, one colour); the two escrow ▶ needs-you rows were the only ▶ rows not bold (bold now); two on-screen purpose lines still promised "orange" and "phosphor" (reworded to the law's words); the log server and PanelFrame still painted bus "completed" events SEAL green (done is not sealed); LogRelay's "awaiting first seal" placeholder glowed green without a receipt (grey until one exists); the LogsPanel paused-tail line and the ModelExplorer proof button were re-pointed; the logger's [WARN] is bold.

Recorded, not fixed here: the legacy App path still paints off/absent states REFUSE red at three sites (layout.tsx docker/comfy down, OptionsPanel [OFF]/[MISSING], FilesPanel "uninitialized") — the v1 shell's keep-or-retire decision (C2) owns them; HOME's ACTIVITY card and the header bypass the CHAT dim palette; a few flattened pairs (think/resp, waiting/warn glyphs) are carried by their words.

**Evidence of colour.** Text captures strip escapes and cannot show a colour checkpoint, so the capture script now keeps an escape-preserving copy of every frame (`<name>.ansi`, FORCE_COLOR exported before tsx) with its own hash in the manifest; the manifest carries the checkpoint and commit. The fresh HOME frame at 120×40 emits exactly four colours: white, grey-3, SEAL green, grey-2.

## Gates

| gate | result |
|---|---|
| tsc · tsgo | exit 0 |
| design-contract | 21/21 (two new cases: the named outside surfaces, chatpage.html ⊂ law) |
| tui-color-contract · receipt · home-journey · chain-pane · header-width · tui-keyboard · library-pane · warroom2 | green (66 tests) |
| full vitest | 93 files / 520 tests passed, 5 files / 15 tests skipped (2 new gate cases over C1a's 518) |
| captures 120/80 | marker c1b1-d9b4895: 40 text frames + 40 escape-preserving frames, fresh + operator (chain copy; the pinned store did not grow), 0/40 exceed their grid, privacy 0; text differs from C1a only where a word or glyph changed |
| privacy on the changed files | 0 at any severity |

## Report

| check | result | seal id |
|---|---|---|
| warn sites re-pointed (attention white, not-yet grey-3) | 35 sites, 0 amber left | ui.v3.c1b1 |
| decorative seal sites re-pointed | 17 sites + 3 found by review; seal only on chain-confirmed | ui.v3.c1b1 |
| review must-fix | 12 applied, 0 open; 3 legacy sites recorded for C2 | — |
| outside surfaces re-pointed and gated | 9 files gated; 2 declared debt | ui.v3.c1b1 |
| captures 120/80 | 40 text + 40 escape-kept frames · 0 over grid · privacy 0 | manifest 6373f0f12b91 |
| tsc · tsgo · vitest | 0 · 0 · 520/520 | — |

arsenal debt: n/a (unchanged).

## §16

Started 09:07:35Z, code and fast gates green 09:13Z, captured 09:15Z, review returned 09:19Z, twelve fixes and re-verification done 09:26Z: 19 minutes against 15. As at C1a the overrun is the adversarial review round; a code checkpoint that carries its own review does not fit 15 minutes. Recorded in checkpoints/C1b-1.json; C1b-2 follows.

**HOLD.**
