# S1–S4 resumed evidence review

The bounded queue passed S1 evidence binding, S2 native view grounding, S3 source-CLI route admission, and S4 normal revision plus signal-assisted local controller recovery. This is a public evidence review package, not a complete installed runtime or a finished UI demo. S5 and S6 remain incomplete.

| Gate | Result | Seal |
|---|---|---|
| S1 evidence binding | One Granite case passed strict 1/1 with a constrained actual-handle enum and observed successful cite(handle_id). | rc_mu260gn3_rze5 |
| S2 native view | Generated box, native camera-Z depth and object ID aligned to the declared sampled geometry/raster checks. Density remains unknown; no physical measurement claim. | rc_mu2666lr_7wft |
| S3 route graph | Source CLI planned the bounded graph; generated-to-physical provenance and shared-backend independence requests were refused. Compiled CLI was not rebuilt. | rc_mu272kea_e4ge |
| S4 normal | One native bore revision, separate-process reopened readback, source preserved and duplicate submission deduplicated. | rc_mu2c7z0b_ufmh |
| S4 recovery | Controller SIGKILL during native execution, same persisted workflow and job, explicit reconnect event, completed submit retained, one edit and fresh readback. | rc_mu2c98lz_igd0 |
| S4 paired verification | Both receipts, artifact bindings and paired demo card verified by the coordinating agent. | rc_mu2ca3aa_ozwh |

S2 is evidence for the original source revision only. No fresh voxel or calibrated native view of either S4 candidate is included, and the S2 view must not be relabeled as a revised candidate view. S5 UI binding and S6 integrated demo acceptance are still pending.

The paired demo card is `evidence/s4m-checkpoint/demo-card.json`. The normal and interrupted runs use separate job IDs and candidate revisions. Within the interrupted run, the job ID stays stable across controller reconnection. This was signal-assisted local Cloudflare Workflows recovery; it does not establish automatic restart, native-host crash recovery, power-loss durability or a universal exactly-once guarantee. The completed native submission step was retained and step cache was not reset.

Native geometric acceptance covers an 80 mm bounding box, 12 radial rays at the bore midplane, opposite-ray diameters near 32 mm within 0.05 mm, one connected component and the declared edge-incidence checks. It does not establish full-depth bore uniformity or every vertex-manifold condition. Computational dimensions of generated geometry are allowed; physical dimensions and material density cannot be established by converting that geometry.

Earlier failures remain part of the record: S1 originally failed 0/1 (`rc_mu20rkhh_inhm`); S3's first installed-toolchain gate failed (`rc_mu26enuc_at93`). S4 first failed at unsupported compatibility date before native submission (`rc_mu27ey0a_ia2y`), then at unsupported fetch redirect:error before native submission (`rc_mu2c02vn_cse3`). Selected reports from both S4 failures are retained here. Neither failure is rewritten as success.

The six accepted corrections remain in TRANSFORMS §9. The constrained-enum plus actual cite(handle_id) policy remains in §11; an empty observed-handle set must yield unknown/refusal, and property-name, stale, unobserved or uncited handles are rejected without post hoc label rewriting. This policy does not claim all legacy evidence fields have migrated.

`PUBLIC-DERIVATION.json` maps each selected original to its public bytes. Receipt files here are unsigned reference projections; the coordinating agent verified the private originals. Original seals bind original files, not redacted substitutes. Raw inference messages, full requests, model credentials, native binaries/documents, host stores and logs are omitted. Original manifests retain hashes and paths for omitted artifacts, so this is intentionally an incomplete public bundle. Placeholder-bearing source snapshots are for review and must not be presented as runnable or installed code.

S1 and route implementation files belong to an untracked developed seed in the canonical working tree. The package preserves bounded frozen/pinned source snapshots without staging that seed, replacing shared implementation or claiming a complete dependency tree. S4 snapshots were copied from its sealed implementation directories; no application, model, native worker or remote host was run during packaging.

Source verification in this public package applies only to the selected frozen artifacts and manifest-pinned source copied at packaging time. It makes no claim that every current developed-seed file still matches an earlier S1–S3 manifest.
